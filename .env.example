# MindBridge Environment Configuration

# Supabase Configuration (Required for production)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# AI Service Configuration (Optional - has intelligent fallbacks)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Email Service Configuration (Optional - logs to localStorage if not configured)
VITE_RESEND_API_KEY=your_resend_api_key_here

# Development Notes:
# - The app works perfectly without API keys using smart fallback systems
# - For demo purposes, localStorage is used for data persistence
# - All external services gracefully degrade when not configured
# - Crisis detection and support resources work regardless of API configuration
